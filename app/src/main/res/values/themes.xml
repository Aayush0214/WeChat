<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeChat" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorPrimaryDark">@color/background1</item>
    </style>

    <style name="Theme.WeChat" parent="Base.Theme.WeChat" />

    <!-- Splash Screen light theme -->
    <style name="Splash_Screen" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:background"> @color/low_white_bg</item>
        <item name="android:textColor"> @color/black </item>
        <item name="android:colorPrimaryDark">@color/low_white_bg</item>
    </style>

</resources>